version: '3'
services:
    app:
        restart: unless-stopped
        build:
            context: app
            args:
                - SLACK_API_TOKEN=${SLACK_API_TOKEN}
        volumes:
            - ./db:/var/lib/brayerpot/